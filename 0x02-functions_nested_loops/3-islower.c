#include "main.h"

/**
* islowercase -> checks for lowercase character
* @c: character argumenit
* Return: returns 1 and 0 depending on the condition
*/
int_islower(int c);
{
return (c >= 'a' && c <= 'z');
}
